model Article {
  id          String       @id @default(uuid())
  title       String
  description String?
  tags        ArticleTag[]
  comments    Comment[]
  likes       Like[]
  authorId    String
  author      User         @relation(fields: [authorId], references: [id])
}
